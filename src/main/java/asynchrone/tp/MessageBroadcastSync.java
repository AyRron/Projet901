package asynchrone.tp;

public class MessageBroadcastSync extends Message{

    public MessageBroadcastSync(Object payload, int estampillage, int senderId) {
        super(payload, estampillage, senderId);
    }
}
